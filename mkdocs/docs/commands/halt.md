# Shutdown a Baker Environment
#### Command: `baker halt <VM Name>`

Use `halt` command to shutdown a running Baker virtual machine. 

!!! Tip 
    Use [`status`](./status.md) command to see list of your Baker environments and their state.
